apiVersion: v1
kind: ConfigMap
metadata:
  name: inventory-config
  namespace: default
  labels:
    app: inventory-api
data:
  SPRING_PROFILES_ACTIVE: "prod"
  SERVER_PORT: "8080"
  JWT_EXPIRATION_MS: "86400000"
  DB_URL: "jdbc:postgresql://postgres-service:5432/inventory_db"
